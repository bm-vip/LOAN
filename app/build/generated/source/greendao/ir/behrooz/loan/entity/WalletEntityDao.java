package ir.behrooz.loan.entity;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "Wallet".
*/
public class WalletEntityDao extends AbstractDao<WalletEntity, Long> {

    public static final String TABLENAME = "Wallet";

    /**
     * Properties of entity WalletEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Value = new Property(1, Long.class, "value", false, "VALUE");
        public final static Property Date = new Property(2, java.util.Date.class, "date", false, "DATE");
        public final static Property PersonId = new Property(3, Long.class, "personId", false, "PERSON_ID");
        public final static Property Description = new Property(4, String.class, "description", false, "DESCRIPTION");
        public final static Property Status = new Property(5, boolean.class, "status", false, "STATUS");
        public final static Property CashId = new Property(6, Long.class, "cashId", false, "CASH_ID");
    }


    public WalletEntityDao(DaoConfig config) {
        super(config);
    }
    
    public WalletEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"Wallet\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"VALUE\" INTEGER," + // 1: value
                "\"DATE\" INTEGER," + // 2: date
                "\"PERSON_ID\" INTEGER," + // 3: personId
                "\"DESCRIPTION\" TEXT," + // 4: description
                "\"STATUS\" INTEGER NOT NULL ," + // 5: status
                "\"CASH_ID\" INTEGER);"); // 6: cashId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"Wallet\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, WalletEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long value = entity.getValue();
        if (value != null) {
            stmt.bindLong(2, value);
        }
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(3, date.getTime());
        }
 
        Long personId = entity.getPersonId();
        if (personId != null) {
            stmt.bindLong(4, personId);
        }
 
        String description = entity.getDescription();
        if (description != null) {
            stmt.bindString(5, description);
        }
        stmt.bindLong(6, entity.getStatus() ? 1L: 0L);
 
        Long cashId = entity.getCashId();
        if (cashId != null) {
            stmt.bindLong(7, cashId);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, WalletEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long value = entity.getValue();
        if (value != null) {
            stmt.bindLong(2, value);
        }
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(3, date.getTime());
        }
 
        Long personId = entity.getPersonId();
        if (personId != null) {
            stmt.bindLong(4, personId);
        }
 
        String description = entity.getDescription();
        if (description != null) {
            stmt.bindString(5, description);
        }
        stmt.bindLong(6, entity.getStatus() ? 1L: 0L);
 
        Long cashId = entity.getCashId();
        if (cashId != null) {
            stmt.bindLong(7, cashId);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public WalletEntity readEntity(Cursor cursor, int offset) {
        WalletEntity entity = new WalletEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // value
            cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)), // date
            cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3), // personId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // description
            cursor.getShort(offset + 5) != 0, // status
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6) // cashId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, WalletEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setValue(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setDate(cursor.isNull(offset + 2) ? null : new java.util.Date(cursor.getLong(offset + 2)));
        entity.setPersonId(cursor.isNull(offset + 3) ? null : cursor.getLong(offset + 3));
        entity.setDescription(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setStatus(cursor.getShort(offset + 5) != 0);
        entity.setCashId(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(WalletEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(WalletEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(WalletEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
